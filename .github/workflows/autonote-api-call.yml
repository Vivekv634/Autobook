name: AutoNote api call

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  api-call:
    runs-on: ubuntu-latest

    steps:
      - name: Make API Call
        run: curl -X POST "https://autobook1.vercel.app/api/utils/autonotes-cron-job"
